# Frozen. Do not overwrite.

These pkls are the 2025 demo ensemble and Help SHAP hashes.
2026 live spread is `cowork_OS/spread_v3_beta/` (`artifacts/prod_ensemble.joblib` after publish).
A write here is a defect.
